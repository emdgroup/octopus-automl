"""Init."""

# Note: Imports are intentionally not performed at package level to avoid
# eager loading of heavy dependencies (torch, etc.) until actually needed.
# Models should be imported directly from their modules when required.
